This is a python package that represents and handles spectra
It contains the realization of spectrum and peak in the code,
Also, the package contains tools for peak analysis, finding peak domain or centers and also peak analysis.

